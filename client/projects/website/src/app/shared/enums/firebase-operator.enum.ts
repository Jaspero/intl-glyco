export enum FirebaseOperator {
  Equal = '==',
  LargerThen = '>',
  SmallerThen = '<',
  LargerThenOrEqual = '>=',
  SmallerThenOrEqual = '<=',
  ArrayContains = 'array-contains',
  ArrayContainsAny = 'array-contains-any',
  In = 'in'
}
